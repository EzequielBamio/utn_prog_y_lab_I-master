/*
 ============================================================================
Pedir el ingreso de 10 números enteros entre -1000 y 1000. Determinar:


Cantidad de positivos y negativos.
EsPositivo: 1: Positivo 0: cero -1: Negativo


Sumatoria de los pares.
El mayor de los impares.
ValidarPariedad: 1: Par 0: Impar

Listado de los números ingresados.
Listado de los números pares.
Listado de los números de las posiciones impares.
Se deberán utilizar funciones y vectores.
 ============================================================================
 */

#include <stdlib.h>
#include "utn.h"

int main(void) {

	setbuf(stdout,NULL);


	float a = 5.2;
	int b;
	int q;

	q = b - a;

	printf("%d", q);



		/*int listaNumeros[10];
		int cantidadPositivos;
		int cantidadNegativos;
		int sumaPares;
		int mayorImpar;

		CargarNumeros(listaNumeros, 10);

		cantidadPositivos = ContadorPositivos(listaNumeros, 10);
		printf("La cantidad de Positivos es: %d\n", cantidadPositivos);

		cantidadNegativos = ContadorNegativos(listaNumeros, 10);
		printf("La cantidade de Negativos es: %d\n", cantidadNegativos);

		sumaPares = SumarPares(listaNumeros, 10);
		printf("La suma de los Pares es: %d\n", sumaPares);

		mayorImpar = MayorImpares(listaNumeros, 10);
		if(mayorImpar != -1)
		{
			printf("El mayor de los Impares es: %d\n", mayorImpar);
		}else
		{
			pritnf("No se ingresaron impares");
		}


		MostrarNumerosIngresados(listaNumeros, 10);
		MostrarNumerosPares(listaNumeros, 10);
		MostrarNumerosImpares(listaNumeros, 10);*/


	return EXIT_SUCCESS;
}
